# Dependencies and build outputs (handled by multi-stage build)
node_modules
.output
.nitro
dist
.cache
.tanstack
.source

# Git and IDE
.git
.gitignore
.github

# Env (keep .env.example if you need it)
.env
!.env.example

# Docs/tests (not needed for build)
test-results
playwright-report
blob-report
playwright
README.md
LICENSE

# Do NOT ignore: source.config.ts, vite.config.ts, tsconfig.json, content/, public/, src/
